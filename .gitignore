# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# database
/prisma/db.sqlite
/prisma/db.sqlite-journal

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
# do not commit any .env files to git, except for the .env.example file. https://create.t3.gg/en/usage/env-variables#using-environment-variables
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# Env vars
.env*
!.env.example

# SSL certificates
certs
ssl
*.key
*.crt

# AI output
output

# Node stuff
dist
node_modules
.vscode

# ROS:chrome extension generator GITIGNORE elements
temp
.tmp
.sass-cache
app/bower_components
test/bower_components
package

# ROS: Our usual GITIGNORE elements
# Logs
logs
*.log

# ROS: Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
# Including these now that the build is no longer public, so that I don't have to do
#  npm install on remote installs.
#
web-site/node_modules/
web-site/jspm_packages/
solidity/node_modules/
solidity/jspm_packages/
solidity/build/

# Typescript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# ROS: Imported AngularJS ignores from PubNub AngularJS tutorial.
# ROS: Including these now that the build is no longer public, so that I don't have to do
# ROS:  npm install on remote installs.
#

# node_modules
react/node_modules
react-native/node_modules
angular/flowtron/electron/node_modules

# ROS: Don't add Webstorm meta files.
.idea
.idea*

# ROS: Don't commit the Browserified file.
# ROS:  YES, commit them so we can use them in an emergency.
# ROS: web-site/public/javascripts/browserified-files/

# ROS: Don't add public test directory, large files in there.
server/public/audio/test/

# ROS: Don't add large video files.
server/public/video/git-ignore/

# ROS: Don't add the dev-assets directory
server/public/dev-assets/

# ROS: Don't add the diretory for temporary images
server/image-temp/

# ROS: Don't include the AI model data directory.
models
